anglesMap = containers.Map('KeyType', 'double', 'ValueType', 'any');

Hrtf.fillAnglesMap(anglesMap);
keys = keys(anglesMap);

radius = 1;
figure();
for i = keys
    for j = anglesMap(i)
        
        
    end
end